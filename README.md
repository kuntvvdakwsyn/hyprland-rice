# hyprland rice

# For arch:
- Install packages:
``` bash
sudo pacman -S fastfetch hyprland swaybg nvim evince foot rofi waybar ipgrep fd gcc make unzip curl tar nodejs npm ttf-jetbrains-mono-nerd
```
- Make dir - configs:
``` bash
cd ~/.config
mkdir foot rofi hypr waybar
```
